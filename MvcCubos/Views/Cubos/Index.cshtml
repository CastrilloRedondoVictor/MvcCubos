@model IEnumerable<MvcCubos.Models.Cubo>

@{
    ViewData["Title"] = "Index";
}

<h1 class="text-center mb-4">Cubos</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Nuevo Cubo</a>
    <a asp-action="Favoritos" class="btn btn-primary">Ver favoritos</a>
</p>

<div class="container">
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-4 mb-4">
                <div class="card shadow-sm">
                    <img src="~/images/@item.Imagen" class="card-img-top img-fluid" alt="Imagen de @item.Nombre" style="height: 200px; object-fit: contain;">
                    <div class="card-body">
                        <h5 class="card-title">@item.Nombre</h5>
                        <h6 class="card-subtitle text-muted">@item.Marca - @item.Modelo</h6>
                        <p class="card-text mt-2"><strong>Precio:</strong> @item.Precio €</p>
                        <div class="d-flex justify-content-between">

                            <a asp-action="Index" asp-route-idFavorito="@item.IdCubo" class="btn btn-danger btn-sm">Favoritos</a>
                            <a asp-action="Edit" asp-route-id="@item.IdCubo" class="btn btn-warning btn-sm">Editar</a>
                            <a asp-action="Details" asp-route-id="@item.IdCubo" class="btn btn-info btn-sm">Detalles</a>
                            <a asp-action="Delete" asp-route-id="@item.IdCubo" class="btn btn-danger btn-sm">Eliminar</a>
                        </div>
                        <div class="mt-3 text-center">
                            <a asp-action="AddToCart" asp-route-id="@item.IdCubo" class="btn btn-success btn-sm w-100">
                                Añadir al Carrito
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

